# Monitoring and Setup Workflow Visual Verification

Desktop verification confirmed that Monitoring presents the pending-approval, expiry, current-snapshot, and sync-needed metrics with a clear zero-state; the Guided setup journey is visually ordered and readable; and the rejected GitHub OAuth state provides a distinct retry path without overstating connection success.

Mobile verification at 375px confirmed that monitoring metrics stack cleanly, the setup milestones retain their sequence and action controls, and the rejected OAuth card remains legible with its retry control. The callback example remains intentionally accompanied by published-domain guidance because a local development callback is not suitable for production authorization.
